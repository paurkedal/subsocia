true: bin_annot

"lib": include

<bin/*>: thread, \
    package(caqti), \
    package(caqti-dynload), \
    package(caqti-lwt), \
    package(cmdliner), \
    package(config-file), \
    package(extunix), \
    package(lwt.ppx), \
    package(panograph), \
    package(uri), \
    package(yojson), \
    use_subsocia, use_subsocia-data
<lib/*.ml*>: \
    package(extunix), \
    package(lwt), \
    package(lwt.log), \
    package(lwt.ppx), \
    package(panograph), \
    package(yojson)
<lib-data/*.ml*>: thread, \
    package(caqti), \
    package(caqti-lwt), \
    package(config-file), \
    package(findlib), \
    package(findlib.dynload), \
    package(lwt.ppx), \
    package(panograph), \
    package(prime.unstable), \
    package(react), \
    use_subsocia
<tests/*>: thread, \
    package(caqti), package(caqti-lwt), package(caqti-dynload), \
    package(config-file), \
    package(extunix), \
    package(lwt.ppx), \
    package(oUnit), \
    package(panograph), \
    package(yojson), \
    use_subsocia, use_subsocia-data
