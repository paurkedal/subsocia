true: bin_annot, eliom_ppx

"lib": include

<bin/*>: thread, \
    package(caqti.v1), package(caqti-lwt.v1), package(caqti-dynload), \
    package(cmdliner), \
    package(config-file), \
    package(extunix), \
    package(panograph), \
    package(rpclib), \
    package(uri), \
    package(yojson), \
    use_subsocia, use_subsocia-data
<lib/*.ml*>: \
    package(extunix), \
    package(lwt), \
    package(lwt.log), \
    package(panograph), \
    package(rpclib), \
    package(yojson)
<lib/data/*.ml*>: thread, \
    package(caqti.v1), package(caqti-lwt.v1), \
    package(config-file), \
    package(panograph), \
    package(prime.unstable), \
    package(react), \
    use_subsocia
<web/server/*.ml*>: thread, \
    package(config-file), \
    package(eliom.server), \
    package(panograph.server), \
    use_subsocia, use_subsocia-data
<web/client/*>: \
    package(eliom.client), \
    package(panograph.client), \
    use_subsocia
<tests/*>: thread, \
    package(caqti.v1), package(caqti-lwt.v1), \
    package(config-file), \
    package(extunix), \
    package(lwt.ppx), \
    package(oUnit), \
    package(panograph), \
    package(rpclib), \
    package(yojson), \
    use_subsocia, use_subsocia-data
