(jbuild_version 1)

(executable
 ((name testsuite)
  (preprocess (pps (lwt.ppx)))
  (libraries
   (caqti-dynload
    lwt.ppx
    oUnit
    subsocia
    subsocia.data))))

(alias
 ((name runtest)
  (deps (testsuite.exe))
  (action (run ${<}))))
