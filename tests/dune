(executable
  (name testsuite)
  (preprocess (pps lwt_ppx))
  (libraries
    alcotest
    caqti-dynload
    subsocia
    subsocia.data))

(rule
  (alias runtest)
  (deps testsuite.exe)
  (action (run %{deps})))
